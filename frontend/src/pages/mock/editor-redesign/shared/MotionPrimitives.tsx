/**
 * 共享 Motion 原语（Mock 页面用）
 * 已提取至生产模块，此处保留 re-export 以兼容现有 mock 页面导入路径
 */

export {
  springDefault,
  springPanel,
  springQuick,
  fadeSlideUp,
  fadeSlideRight,
  fadeSlideLeft,
  scaleIn,
  PressableButton,
  SlidePanel,
  listItemLayout,
  useMotionConfig,
} from '@/components/teaching-document/motion'
