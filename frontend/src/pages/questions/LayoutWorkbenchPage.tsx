import { useCallback, useEffect, useMemo, useRef, useState, type ReactNode } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { AlertTriangle, ChevronDown, ChevronLeft, ChevronRight, Eye, FileStack, GripVertical, LoaderCircle, PanelLeftClose, PanelRightClose, Redo2, RotateCcw, Save, Undo2 } from 'lucide-react'
import { collectionsApi } from '@/api/collections'
import { layoutDraftsApi, type FigureAlignment, type FigurePlacement, type LayoutDraft, type LayoutPreview, type PaperLayout, type QuestionLayout } from '@/api/layoutDrafts'
import type { Basket } from '@/types'
import { LayoutQuickPreview, type RealtimeLayoutResult } from '@/components/questions/LayoutQuickPreview'
import { choiceSuggestion, defaultSolutionAnswerAreaHeightCm, figureIdOf, hydrateLayout, isSolutionQuestion, moveWithinSection, orderedQuestions, patchQuestion } from '@/components/questions/layoutWorkbenchModel'
import { isChoiceQuestionType, parseChoiceQuestion } from '@/utils/questionDisplay'

const iconButton = 'inline-flex size-8 items-center justify-center rounded-md text-zinc-500 hover:bg-zinc-100 disabled:opacity-30 dark:hover:bg-zinc-900'
const control = 'h-8 w-full rounded-md border border-zinc-200 bg-white px-2 text-xs dark:border-zinc-800 dark:bg-zinc-950'

export default function LayoutWorkbenchPage() {
  const { id = '', draftId = '' } = useParams(); const navigate = useNavigate()
  const [basket, setBasket] = useState<Basket | null>(null); const [draft, setDraft] = useState<LayoutDraft | null>(null); const [layout, setLayout] = useState<PaperLayout | null>(null)
  const [selectedId, setSelectedId] = useState(''); const [leftOpen, setLeftOpen] = useState(true); const [rightOpen, setRightOpen] = useState(true)
  const [saveState, setSaveState] = useState<'saved' | 'dirty' | 'saving' | 'error'>('saved'); const [preview, setPreview] = useState<LayoutPreview | null>(null); const [message, setMessage] = useState('')
  const [variant,setVariant]=useState<'student'|'teacher'>('student'); const [activePage,setActivePage]=useState(1); const [realtime,setRealtime]=useState<RealtimeLayoutResult|null>(null)
  const undoStack = useRef<PaperLayout[]>([]); const redoStack = useRef<PaperLayout[]>([]); const timer = useRef<number | undefined>(undefined); const previewBusy=useRef(false)

  useEffect(() => { Promise.all([collectionsApi.getCollection(id), layoutDraftsApi.get(draftId)]).then(([b, response]) => { const d=response.draft; const hydrated = hydrateLayout(b, d.layout); setBasket(b); setDraft(d); setLayout(hydrated); setSelectedId(hydrated.questions[0]?.relationId || '') }).catch(e => setMessage(e instanceof Error ? e.message : String(e))) }, [draftId, id])
  const save = useCallback(async (next: PaperLayout) => { if (!draft) return; setSaveState('saving'); try { const response=await layoutDraftsApi.save(draft.id,{revision:draft.revision,layout:next}); setDraft(response.draft); setSaveState('saved'); return response.draft } catch (e) { setSaveState('error'); setMessage(e instanceof Error ? e.message : String(e)) } }, [draft])
  const generateExactPreview=useCallback(async(base:LayoutDraft)=>{if(previewBusy.current)return;previewBusy.current=true;try{const synced=await layoutDraftsApi.refreshContent(base.id,base.revision);const refreshed=synced.draft;setDraft(refreshed);const response=await layoutDraftsApi.preview(refreshed.id,refreshed.revision);setPreview(response.preview);setDraft(current=>current?.id===refreshed.id?{...refreshed,preview:response.preview}:current)}catch(e){setMessage(e instanceof Error?e.message:String(e))}finally{previewBusy.current=false}},[])
  const scheduleSave = useCallback((next: PaperLayout) => { setSaveState('dirty'); clearTimeout(timer.current); timer.current = window.setTimeout(() => void save(next), 900) }, [save])
  const change = useCallback((next: PaperLayout) => { if (!layout) return;const normalized={...next,questions:next.questions.map(question=>{const previous=layout.questions.find(item=>item.relationId===question.relationId);return question.answerAreaHeight!==undefined&&question.answerAreaHeight!==previous?.answerAreaHeight&&next.solutionPageStrategy===layout.solutionPageStrategy?{...question,answerAreaManual:true}:question})};if(JSON.stringify(normalized)===JSON.stringify(layout))return;undoStack.current.push(layout);redoStack.current=[];setLayout(normalized);setSaveState('dirty');clearTimeout(timer.current);timer.current=window.setTimeout(()=>void save(normalized),900) }, [layout, save])
  useEffect(() => { const block = (e: BeforeUnloadEvent) => { if (saveState !== 'saved') e.preventDefault() }; addEventListener('beforeunload', block); return () => removeEventListener('beforeunload', block) }, [saveState])
  useEffect(()=>{if(saveState!=='saved'||!draft)return;if(draft.preview.revision===draft.revision&&draft.preview.status!=='idle')return;const handle=window.setTimeout(()=>void generateExactPreview(draft),650);return()=>window.clearTimeout(handle)},[saveState,draft?.id,draft?.revision,draft?.preview.revision,draft?.preview.status,generateExactPreview])
  useEffect(()=>{if(!draft||!['queued','rendering'].includes(draft.preview.status))return;const handle=window.setInterval(()=>{void layoutDraftsApi.status(draft.id).then(next=>{setPreview(next);setDraft(current=>current?{...current,preview:next}:current)}).catch(e=>setMessage(e instanceof Error?e.message:String(e)))},750);return()=>window.clearInterval(handle)},[draft?.id,draft?.preview.status])
  const entries = useMemo(() => basket && layout ? orderedQuestions(basket, layout) : [], [basket, layout]); const selected = entries.find(e => e.layout.relationId === selectedId)
  const syncVisiblePage = useCallback((page:number) => {
    setActivePage(page)
    const pages=draft?.preview.questionPages?.[variant]||{}
    const selectedPage=pages[selectedId]
    if(selectedPage&&selectedPage.startPage<=page&&selectedPage.endPage>=page)return
    const visible=entries.find(entry=>{const range=pages[entry.layout.relationId];return range&&range.startPage<=page&&range.endPage>=page})
    if(visible)setSelectedId(visible.layout.relationId)
  },[draft?.preview.questionPages,entries,selectedId,variant])
  function navigatePage(page:number){setActivePage(page);requestAnimationFrame(()=>document.getElementById(`exact-preview-page-${page}`)?.scrollIntoView({behavior:'smooth',block:'start'}))}
  function selectQuestion(id:string){setSelectedId(id);const page=draft?.preview.questionPages?.[variant]?.[id]?.startPage;if(page)navigatePage(page)}
  function undo() { if (!layout) return; const v = undoStack.current.pop(); if (v) { redoStack.current.push(layout); setLayout(v); scheduleSave(v) } }
  function redo() { if (!layout) return; const v = redoStack.current.pop(); if (v) { undoStack.current.push(layout); setLayout(v); scheduleSave(v) } }
  function applyPageEqualization(startRelationId:string,count:2|3|null){if(!layout||!realtime)return;const restore=(item:QuestionLayout):QuestionLayout=>({...item,answerAreaHeight:item.equalizedPreviousAnswerAreaHeight,answerAreaManual:item.equalizedPreviousAnswerAreaManual,equalizedAnswerAreaHeight:undefined,equalizedPageBreakBefore:undefined,equalizedGroupId:undefined,equalizedGroupSize:undefined,equalizedPreviousAnswerAreaHeight:undefined,equalizedPreviousAnswerAreaManual:undefined});const solutions=entries.filter(entry=>isSolutionQuestion(entry.question));const startIndex=solutions.findIndex(entry=>entry.layout.relationId===startRelationId);if(startIndex<0)return;const current=layout.questions.find(item=>item.relationId===startRelationId);const currentGroup=current?.equalizedGroupId||startRelationId;if(!count){change({...layout,questions:layout.questions.map(item=>item.equalizedGroupId===currentGroup?restore(item):item)});return}const grouped=solutions.slice(startIndex,startIndex+count);const selectedIds=new Set(grouped.map(entry=>entry.layout.relationId));const conflictingGroups=new Set(grouped.map(entry=>entry.layout.equalizedGroupId).filter(Boolean));const measured=new Map(realtime.questionMeasurements.map(item=>[item.questionId,item.contentHeightPx]));const pagePx=(draft.templateSpec.page.heightMm-draft.templateSpec.page.marginTopMm-draft.templateSpec.page.marginBottomMm-draft.templateSpec.header.heightMm)*96/25.4;const slotPx=pagePx/count;const cleared=layout.questions.map(item=>conflictingGroups.has(item.equalizedGroupId)?restore(item):item);change({...layout,questions:cleared.map(item=>{if(!selectedIds.has(item.relationId))return item;const content=measured.get(item.relationId)||0;const equalized=Math.max(.8,Math.min(12,(slotPx-content-28)/(96/2.54)));return {...item,equalizedPreviousAnswerAreaHeight:item.answerAreaHeight,equalizedPreviousAnswerAreaManual:item.answerAreaManual,answerAreaHeight:undefined,answerAreaManual:false,equalizedAnswerAreaHeight:Number(equalized.toFixed(1)),equalizedPageBreakBefore:item.relationId===startRelationId,equalizedGroupId:startRelationId,equalizedGroupSize:count}})})}
  async function exactPreview() { if (!layout || !draft) return; const saved=saveState !== 'saved' ? await save(layout) : draft; if(saved&&!(saved.preview.revision===saved.revision&&saved.preview.status==='ready'))await generateExactPreview(saved) }
  if (!basket || !layout || !draft) return <div className="p-8 text-sm text-zinc-500">{message || '正在载入排版草稿...'}</div>
  return <div className="-m-4 flex h-[calc(100vh-65px)] min-h-0 flex-col bg-zinc-100/60 md:-m-6 dark:bg-zinc-950">
    <header className="flex min-h-14 shrink-0 flex-wrap items-center justify-between gap-2 border-b border-zinc-200 bg-white px-3 py-2 dark:border-zinc-800 dark:bg-zinc-950"><div className="flex min-w-0 items-center gap-2"><button title="返回草稿列表" className={iconButton} onClick={() => navigate('/questions/layout-drafts')}><ChevronLeft className="size-4" /></button><div className="min-w-0"><div className="flex items-center gap-2"><b className="truncate text-sm">{draft.name || basket.title}</b><span className="hidden rounded-md border border-emerald-200 bg-emerald-50 px-2 py-0.5 text-[10px] font-medium text-emerald-700 sm:inline-flex">PDF 精确编辑</span></div><p className="text-[10px] text-zinc-400">{entries.length} 题 · {exactVariant(draft,variant)?.pageCount||'-'} 页 · revision {draft.revision}</p></div></div><div className="flex items-center gap-1"><div className="flex items-center rounded-md border border-zinc-200 bg-zinc-100 p-0.5 dark:border-zinc-800 dark:bg-zinc-900"><button className={`h-7 rounded px-2 text-[11px] ${variant==='student'?'bg-white font-medium shadow-sm dark:bg-zinc-800':''}`} onClick={()=>setVariant('student')}>学生版</button><button className={`h-7 rounded px-2 text-[11px] ${variant==='teacher'?'bg-white font-medium shadow-sm dark:bg-zinc-800':''}`} onClick={()=>setVariant('teacher')}>教师版</button></div><div className="hidden items-center rounded-md border border-zinc-200 p-0.5 sm:flex dark:border-zinc-800"><button title="撤销" className={iconButton} onClick={undo}><Undo2 className="size-4" /></button><button title="重做" className={iconButton} onClick={redo}><Redo2 className="size-4" /></button></div><button title="切换大纲" className={`${iconButton} hidden lg:inline-flex`} onClick={()=>setLeftOpen(!leftOpen)}><PanelLeftClose className="size-4"/></button><button title="切换属性" className={`${iconButton} hidden lg:inline-flex`} onClick={()=>setRightOpen(!rightOpen)}><PanelRightClose className="size-4"/></button><button className="ml-1 inline-flex h-8 items-center gap-1.5 rounded-md border border-zinc-200 bg-white px-3 text-xs font-medium hover:bg-zinc-50 dark:border-zinc-800 dark:bg-zinc-950" onClick={() => void save(layout)}><Save className="size-3.5" />保存</button><button className="inline-flex h-8 items-center gap-1.5 rounded-md bg-zinc-900 px-3 text-xs font-medium text-white dark:bg-zinc-50 dark:text-zinc-900" onClick={() => void exactPreview()}><Eye className="size-3.5" />更新精确页面</button></div></header>
    {message ? <div className="flex items-center gap-2 border-b border-amber-200 bg-amber-50 px-4 py-2 text-xs text-amber-800"><AlertTriangle className="size-3.5" />{message}<button className="ml-auto" onClick={() => setMessage('')}>关闭</button></div> : null}
    <div className="pointer-events-none fixed left-[-30000px] top-0 w-[900px] opacity-0" aria-hidden><LayoutQuickPreview entries={entries} selectedId="" zoom={1} onSelect={()=>{}} title={basket.title} templateSpec={draft.templateSpec} variant={variant} onLayout={setRealtime}/></div>
    <div className="flex min-h-0 flex-1">{leftOpen ? <Outline entries={entries} selectedId={selectedId} onSelect={selectQuestion} onMove={(a,b) => change(moveWithinSection(layout,basket,a,b))} /> : null}<main className="min-w-0 flex-1 overflow-auto px-3 lg:px-6"><ExactPageToolbar draft={draft} entries={entries} variant={variant} page={activePage} onPage={navigatePage} onEqualize={applyPageEqualization}/><ExactPreviewCanvas draft={draft} variant={variant} page={activePage} onVisiblePage={syncVisiblePage}/></main>{rightOpen ? <div className="hidden xl:contents"><Properties selected={selected} onChange={p => selected && change(patchQuestion(layout, selected.layout.relationId, p))} /></div> : null}</div>
    <footer className="flex h-9 shrink-0 items-center justify-between border-t border-zinc-200 bg-white px-4 text-[11px] text-zinc-500 dark:border-zinc-800 dark:bg-zinc-950"><span className="hidden sm:inline">左侧选择或调整题序，右侧修改属性；中间始终显示最终 PDF 结果。</span><span className={saveState==='error'?'text-red-600':saveState==='dirty'?'text-amber-700':''}>{saveState === 'saved' ? `已保存 · r${draft.revision}` : saveState === 'saving' ? '正在保存...' : saveState === 'error' ? '保存失败，本地修改仍保留' : '有未保存修改'}{['queued','rendering'].includes(draft.preview.status)?' · 正在更新精确页面':preview && preview.revision !== draft.revision ? ' · 精确页面已过期' : ''}</span></footer>
  </div>
}

function exactVariant(draft:LayoutDraft,variant:'student'|'teacher'){return draft.preview.variants?.[variant]}
function exactPageCount(draft:LayoutDraft,variant:'student'|'teacher'){const exact=exactVariant(draft,variant);if(exact?.pageCount)return exact.pageCount;return Math.max(1,...Object.values(draft.preview.questionPages?.[variant]||{}).map(item=>item.endPage))}
function ExactPageToolbar({draft,entries,variant,page,onPage,onEqualize}:{draft:LayoutDraft;entries:ReturnType<typeof orderedQuestions>;variant:'student'|'teacher';page:number;onPage:(page:number)=>void;onEqualize:(id:string,count:2|3|null)=>void}){const total=exactPageCount(draft,variant);const pages=draft.preview.questionPages?.[variant]||{};const pageSolutions=entries.filter(entry=>isSolutionQuestion(entry.question)&&pages[entry.layout.relationId]?.startPage===page);const start=pageSolutions[0];const active=start&&start.layout.equalizedGroupId===start.layout.relationId?start.layout.equalizedGroupSize:undefined;return <div className="sticky top-2 z-20 mx-auto mt-3 flex w-fit items-center gap-2 rounded-lg border border-zinc-200 bg-white/95 px-2 py-1.5 text-xs shadow-md backdrop-blur"><button className="h-7 rounded border px-2 disabled:opacity-30" disabled={page<=1} onClick={()=>onPage(Math.max(1,page-1))}>上一页</button><span className="min-w-20 text-center tabular-nums">第 {page} / {total} 页</span><button className="h-7 rounded border px-2 disabled:opacity-30" disabled={page>=total} onClick={()=>onPage(Math.min(total,page+1))}>下一页</button><span className="mx-1 h-5 w-px bg-zinc-200"/>{start?<><span className="text-zinc-500">本页解答题</span>{([['auto','自动'],[2,'一页两题'],[3,'一页三题']] as const).map(([value,label])=><button key={value} className={`h-7 rounded px-2 ${value==='auto'?!active?'bg-zinc-900 text-white':'border':active===value?'bg-zinc-900 text-white':'border'}`} onClick={()=>onEqualize(start.layout.relationId,value==='auto'?null:value)}>{label}</button>)}</>:<span className="text-zinc-400">本页无解答题排版操作</span>}</div>}
function ExactPreviewCanvas({draft,variant,page,onVisiblePage}:{draft:LayoutDraft;variant:'student'|'teacher';page:number;onVisiblePage:(page:number)=>void}){const exact=exactVariant(draft,variant);const imageKey=exact?.pageImages.join('|')||'';useEffect(()=>{if(!imageKey||typeof IntersectionObserver==='undefined')return;const ratios=new Map<Element,number>();const observer=new IntersectionObserver(records=>{records.forEach(record=>ratios.set(record.target,record.intersectionRatio));let best:Element|undefined;let score=0;ratios.forEach((ratio,node)=>{if(ratio>score){score=ratio;best=node}});const visible=Number((best as HTMLElement|undefined)?.dataset.exactPage||0);if(visible&&score>0.05)onVisiblePage(visible)},{threshold:[0,.1,.25,.5,.75,1],rootMargin:'-80px 0px -35% 0px'});document.querySelectorAll('[data-exact-page]').forEach(node=>observer.observe(node));return()=>observer.disconnect()},[imageKey,onVisiblePage]);if(['queued','rendering'].includes(draft.preview.status))return <div className="flex h-full min-h-[520px] items-center justify-center text-sm text-zinc-500"><LoaderCircle className="mr-2 size-4 animate-spin"/>正在使用最终 PDF 引擎更新预览…</div>;if(draft.preview.status==='failed')return <div className="m-6 rounded-md border border-red-200 bg-red-50 p-4 text-sm text-red-700">{draft.preview.error||'精确预览生成失败。'}</div>;if(exact?.pageImages.length)return <div className="space-y-5 py-5">{exact.pageImages.map((src,index)=><div id={`exact-preview-page-${index+1}`} data-exact-page={index+1} key={src} className="scroll-mt-14"><img src={src} alt={`${variant==='teacher'?'教师版':'学生版'}第 ${index+1} 页`} className="mx-auto w-full max-w-[900px] bg-white shadow-sm ring-1 ring-zinc-200"/></div>)}</div>;if(exact?.pdfUrl){const src=`${exact.pdfUrl}#page=${page}&zoom=page-width`;return <iframe key={src} title={`${variant==='teacher'?'教师版':'学生版'}精确预览`} src={src} className="my-4 h-[calc(100vh-205px)] w-full rounded-md border bg-white"/>}return <div className="flex h-full min-h-[520px] items-center justify-center text-sm text-zinc-500">等待生成精确预览…</div>}

export function outlineText(markdown: string) {
  return String(markdown || '')
    .replace(/<!--[^]*?-->/g, ' ')
    .replace(/<[^>]+>/g, ' ')
    .replace(/\\frac\s*\{([^{}]*)\}\s*\{([^{}]*)\}/g, '$1/$2')
    .replace(/\\sqrt\s*\{([^{}]*)\}/g, '√($1)')
    .replace(/\\(?:text|mathrm|mathbf|mathit|operatorname)\s*\{([^{}]*)\}/g, '$1')
    .replace(/\\(?:left|right|displaystyle|textstyle|quad|qquad|,|;|!)/g, ' ')
    .replace(/\\[a-zA-Z]+/g, '')
    .replace(/[$*_~`#>|]/g, ' ')
    .replace(/!?\[([^\]]*)\]\([^)]*\)/g, '$1')
    .replace(/[{}]/g, '')
    .replace(/\s+/g, ' ')
    .trim()
}

function Outline({ entries, selectedId, onSelect, onMove }: { entries: ReturnType<typeof orderedQuestions>; selectedId: string; onSelect: (id:string)=>void; onMove:(a:string,b:string)=>void }) {
  const [drag, setDrag] = useState('')
  const [collapsed, setCollapsed] = useState<Set<string>>(() => new Set())
  const groups = entries.reduce<Array<{ key: string; name: string; start: number; items: typeof entries }>>((result, entry, index) => {
    const name = entry.question.sectionName || result[result.length - 1]?.name || ''
    const previous = result[result.length - 1]
    if (previous?.name === name) previous.items.push(entry)
    else result.push({ key: `${index}:${name}`, name, start: index, items: [entry] })
    return result
  }, [])
  useEffect(()=>{
    if(!selectedId)return
    const group=groups.find(item=>item.items.some(entry=>entry.layout.relationId===selectedId))
    if(group)setCollapsed(current=>current.has(group.key)?new Set([...current].filter(key=>key!==group.key)):current)
    requestAnimationFrame(()=>document.querySelector<HTMLElement>(`[data-outline-question="${CSS.escape(selectedId)}"]`)?.scrollIntoView({block:'nearest',behavior:'smooth'}))
  },[selectedId])
  function toggleGroup(key: string) {
    setCollapsed(current => {
      const next = new Set(current)
      if (next.has(key)) next.delete(key)
      else next.add(key)
      return next
    })
  }
  return <aside className="hidden w-64 shrink-0 overflow-auto border-r border-zinc-200 bg-white lg:block dark:border-zinc-800 dark:bg-zinc-950">
    <div className="sticky top-0 z-10 flex h-11 items-center gap-2 border-b border-zinc-100 bg-white px-3 dark:border-zinc-900 dark:bg-zinc-950"><FileStack className="size-3.5 text-zinc-400"/><b className="text-xs">题目大纲</b><span className="ml-auto text-[10px] text-zinc-400">{entries.length}</span></div>
    <div className="p-2">{groups.map(group => {
      const isCollapsed = collapsed.has(group.key)
      return <section key={group.key} className="mb-2">
        {group.name ? <button type="button" aria-expanded={!isCollapsed} onClick={() => toggleGroup(group.key)} className="flex w-full items-center gap-1.5 rounded-md px-2 py-2 text-left text-[11px] font-semibold text-zinc-600 transition-colors hover:bg-zinc-100 dark:text-zinc-400 dark:hover:bg-zinc-900">
          {isCollapsed ? <ChevronRight className="size-3.5 shrink-0"/> : <ChevronDown className="size-3.5 shrink-0"/>}<span className="min-w-0 flex-1 truncate">{group.name}</span><span className="text-[10px] font-normal tabular-nums text-zinc-400">{group.items.length} 题</span>
        </button> : null}
        <div className={`grid transition-[grid-template-rows,opacity] duration-200 ease-out ${isCollapsed ? 'grid-rows-[0fr] opacity-0' : 'grid-rows-[1fr] opacity-100'}`}><div className="min-h-0 overflow-hidden"><div className={group.name ? 'pt-1' : ''}>{group.items.map(({ question, layout }, offset) => {
          const i = group.start + offset
          const isSelected = selectedId === layout.relationId
          return <div key={layout.relationId} data-outline-question={layout.relationId} aria-current={isSelected ? 'true' : undefined} draggable onDragStart={() => setDrag(layout.relationId)} onDragOver={e => e.preventDefault()} onDrop={() => onMove(drag, layout.relationId)} onClick={() => onSelect(layout.relationId)} className={`group relative mb-1 flex cursor-pointer gap-2 overflow-hidden rounded-md border px-2 py-2 text-xs transition-[transform,box-shadow,border-color,background-color] duration-200 ease-out hover:-translate-y-0.5 ${isSelected ? 'border-blue-500 bg-blue-50 shadow-md ring-1 ring-blue-500/25 hover:bg-blue-50 dark:border-blue-400 dark:bg-blue-950/40 dark:ring-blue-400/30 dark:hover:bg-blue-950/40' : 'border-zinc-200 bg-white shadow-sm hover:border-zinc-300 hover:shadow-md dark:border-zinc-800 dark:bg-zinc-950 dark:hover:border-zinc-700'}`}>
            {isSelected ? <span aria-hidden="true" className="absolute inset-y-1 left-0 w-0.5 rounded-r bg-blue-600 dark:bg-blue-400"/> : null}
            <GripVertical className="mt-0.5 size-3.5 shrink-0 text-zinc-400"/>
            <div className="min-w-0 flex-1"><div className="flex justify-between gap-2"><b className={`truncate ${isSelected ? 'text-blue-950 dark:text-blue-100' : ''}`}>{i + 1}. {question.item.questionType || '未设题型'}</b><span className={`shrink-0 ${isSelected ? 'text-blue-700 dark:text-blue-300' : 'text-zinc-400'}`}>{question.score || 0}分</span></div><p className={`mt-1 line-clamp-2 leading-5 ${isSelected ? 'text-blue-900/70 dark:text-blue-100/70' : 'text-zinc-500'}`}>{outlineText(question.item.stemMarkdown) || '题干为空'}</p></div>
            <div className="invisible flex flex-col group-hover:visible"><button title="上移" onClick={e => { e.stopPropagation(); if(entries[i - 1]) onMove(layout.relationId, entries[i - 1].layout.relationId) }}>↑</button><button title="下移" onClick={e => { e.stopPropagation(); if(entries[i + 1]) onMove(layout.relationId, entries[i + 1].layout.relationId) }}>↓</button></div>
          </div>
        })}</div></div></div>
      </section>
    })}</div>
  </aside>
}
function Field({ label, children }: { label:string; children:ReactNode }) { return <section className="mb-4 border-b border-zinc-100 pb-4 dark:border-zinc-900"><label className="mb-2 block text-[11px] font-medium text-zinc-500">{label}</label>{children}</section> }
function Properties({ selected, onChange }: { selected?: ReturnType<typeof orderedQuestions>[number]; onChange:(p:Partial<QuestionLayout>)=>void }) { if(!selected)return <aside className="w-72 bg-white p-4 text-xs">请选择题目</aside>; const {question,layout}=selected; const hasChoices=isChoiceQuestionType(question.item.questionType)&&Boolean(parseChoiceQuestion(question.item.stemMarkdown)); const solution=isSolutionQuestion(question); const suggestion=hasChoices?choiceSuggestion(question.item.stemMarkdown):null; const updateFigure=(id:string,p:Record<string,unknown>)=>onChange({figures:[...layout.figures.filter(x=>x.figureId!==id),{figureId:id,placement:'auto' as const,...layout.figures.find(x=>x.figureId===id),...p}]}); const automaticBreak=layout.equalizedPageBreakBefore===true; return <aside className="w-72 shrink-0 overflow-auto bg-white p-4 dark:bg-zinc-950"><b className="mb-4 block text-xs">题目属性</b>{hasChoices&&suggestion?<Field label="选项布局"><select className={control} value={layout.choiceLayout} onChange={e=>onChange({choiceLayout:e.target.value as QuestionLayout['choiceLayout']})}><option value="auto">自动（建议{suggestion.layout==='four'?'四栏':suggestion.layout==='two'?'两栏':'单栏'}）</option><option value="four">四栏</option><option value="two">两栏</option><option value="one">单栏</option></select><p className="mt-1 text-[10px] text-zinc-400">{suggestion.reason}</p></Field>:<Field label="题型"><p className="text-xs text-zinc-500">{question.item.questionType||'未设题型'}，无选项布局。</p></Field>}<Field label="分页"><label className="mb-2 flex gap-2 text-xs"><input type="checkbox" checked={Boolean(layout.pageBreakBefore||automaticBreak)} onChange={e=>onChange({pageBreakBefore:e.target.checked,equalizedPageBreakBefore:e.target.checked?layout.equalizedPageBreakBefore:false})}/>题前分页{automaticBreak?'（等高排版自动设置）':''}</label><label className="flex gap-2 text-xs"><input type="checkbox" checked={layout.keepTogether!==false} onChange={e=>onChange({keepTogether:e.target.checked})}/>尽量保持整题</label>{automaticBreak?<p className="mt-2 text-[10px] leading-4 text-amber-700">取消“题前分页”可移除本题的隐藏自动分页，同时保留当前答题区高度。</p>:null}</Field>{solution?<Field label="答题区高度"><input className="w-full" type="range" min="0" max="12" step="0.2" value={layout.answerAreaHeight ?? defaultSolutionAnswerAreaHeightCm} onChange={e=>onChange({answerAreaHeight:Number(e.target.value)||undefined})}/><span className="text-xs">{(layout.answerAreaHeight ?? defaultSolutionAnswerAreaHeightCm).toFixed(1)}cm</span></Field>:null}{question.item.figures.map((f,i)=>{const id=figureIdOf(f,i),c=layout.figures.find(x=>x.figureId===id);return <Field key={id} label={`题图 ${i+1}`}><select className={control} value={c?.placement||'auto'} onChange={e=>updateFigure(id,{placement:e.target.value as FigurePlacement})}><option value="auto">自动</option><option value="before-choices">选项前</option><option value="side-right">选项右侧</option><option value="side-left">选项左侧</option><option value="after-choices">选项后</option></select><input className="mt-2 w-full" type="range" min="20" max="100" step="5" value={(c?.widthRatio||.45)*100} onChange={e=>updateFigure(id,{widthRatio:Number(e.target.value)/100})}/><span className="text-[11px]">{Math.round((c?.widthRatio||.45)*100)}%</span><select className={`${control} mt-2`} value={c?.alignment||'center'} onChange={e=>updateFigure(id,{alignment:e.target.value as FigureAlignment})}><option value="left">左对齐</option><option value="center">居中</option><option value="right">右对齐</option></select></Field>})}<button className="flex h-8 w-full items-center justify-center gap-1 rounded-md border text-xs" onClick={()=>onChange({choiceLayout:'auto',figures:[],keepTogether:true,pageBreakBefore:false,equalizedPageBreakBefore:false,answerAreaHeight:solution?defaultSolutionAnswerAreaHeightCm:undefined})}><RotateCcw className="size-3.5"/>恢复本题自动排版</button></aside> }
